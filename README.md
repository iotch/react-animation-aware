# react-animation-aware
React component that mounts/unmounts its children depending on CSS animation/transition duration

## Example usage
See [examples](/examples/index.tsx) or clone and run `dist/index.html`